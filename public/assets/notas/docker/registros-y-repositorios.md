## 📦 ¿Qué es un registro (Docker Registry)?

Un registro es un servidor donde se almacenan imágenes de Docker.
Es comparable a GitHub, pero para imágenes de contenedores.

✔️ Ejemplos de registros:

Docker Hub (el más conocido)

GitHub Container Registry (GHCR)

GitLab Container Registry

AWS ECR

Google Container Registry (GCR)

Azure Container Registry (ACR)

Tú puedes usar un registro público o privado, incluso correr uno propio.

## 📁 ¿Qué es un repositorio (Docker Repository)?

Un repositorio es un espacio dentro del registro donde se agrupan todas las versiones (tags) de una imagen.

Es como un repositorio de Git, pero contiene tags de una imagen, no commits.


## Imagen representativa para un registro

![registros-y-repositorios](assets/images/registros-y-repositorios.png)